<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.example.mulcc"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="21" />
    
    <uses-permission android:name="android.permission.BLUETOOTH"/>
	<uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="org.example.mulcc.buttonButtonActivity"
            android:label="@string/button"/>
        <activity
            android:name="org.example.mulcc.gravityButtonActivity"
            android:label="@string/gravity"/>
        <activity
            android:name="org.example.mulcc.rockerButtonActivity"
            android:label="@string/rocker"/>
        <activity
            android:name="org.example.mulcc.canvasButtonActivity"
            android:label="@string/canvas"/>
    </application>

</manifest>
